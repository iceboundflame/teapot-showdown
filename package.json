{
  "name": "teapot-showdown",
  "version": "1.0.0",
  "description": "Match the ghost teapot, beat the clock — a 3D input device mini-game (mouse / SpaceMouse / Rotatrix)",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.11"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "three": "^0.172.0"
  }
}
